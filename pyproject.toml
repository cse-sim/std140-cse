[tool.poetry]
authors = ["Big Ladder Software"]
description = "Set of Standard 140 test cases for the California Simulation Engine (CSE)."
name = "std10-cse"
# packages = [{include = "resnet_multiclimate_results"}] 
repository = "https://github.com/cse-sim/std140-cse" 
version = "0.0.0" # Generated from git on CI

[tool.poetry.dependencies]
dimes = {git = "https://github.com/bigladder/dimes.git"}
mako = "*"
openpyxl = "*"
pandas = "*"
python = ">=3.11,<4.0"
pytz = "*"

[tool.poetry.group.dev.dependencies]
ipykernel = "^6.21.2"

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core>=1.0.0"]
