<%#INTERFACE

parameter "p1", :default=>99
parameter "p2", :default=>12
parameter "p3", :default=>"string"

%>
<%
x = 3
y = x + p2

# these all give warnings correctly:
#foo *bar  # `*' interpreted as argument prefix
#z = .99  # no .<digit> floating literal anymore; put 0 before dot
#def bob(ARG); end  # formal argument cannot be a constant
#def bob(arg, arg); end  # duplicate argument name


#Bob = 9   # doesn't get detected unless another warning is found first  ...?
#Bob = 55

@BAD = 99



insert_template "test3.pxt"

%>

CONTENT
CONTENT
CONTENT

p3 is <%= p3 %>

END
