# Enables command-line completions for Bash while typing in a Terminal console.
# Start typing the beginning of a command name and then press the Tab key to
# automatically complete. Press the Tab key twice to get ambiguous matches.
#
# To use the completions, add this line to your ~/.bash_profile or ~/.bashrc
# configuration file:
#
#   source <path>/etc/bash_completion.d/modelkit-energyplus
#
# Replace <path> with the full directory path to the file.

_modelkit-energyplus()
{
  if [ -z $2 ] ; then
    COMPREPLY=(`modelkit-energyplus help -c`)
  else
    COMPREPLY=(`modelkit-energyplus help -c $2`)
  fi
}
complete -F _modelkit-energyplus modelkit-energyplus
